package com.example;

import java.net.MalformedURLException;
import java.net.URL;

import org.openqa.selenium.remote.DesiredCapabilities;

import io.appium.java_client.AppiumDriver;
import io.appium.java_client.android.AndroidDriver;
import io.appium.java_client.remote.MobileCapabilityType;

/**
 * Unit test for simple App.
 */
public class AppTest {
    /**
     * Rigorous Test :-)
     * 
     * @throws MalformedURLException
     */
    /*
     * public static void main(String[] args) throws MalformedURLException {
     * DesiredCapabilities caps = new DesiredCapabilities();
     * caps.setCapability(MobileCapabilityType.PLATFORM_NAME, "Android");
     * caps.setCapability(MobileCapabilityType.DEVICE_NAME, "LindoFono");
     * caps.setCapability(MobileCapabilityType.AUTOMATION_NAME, "UIAutomator2");
     * caps.setCapability(MobileCapabilityType.UDID, "emulator-5554");
     * // caps.setCapability("avd", "Pixel_3");
     * caps.setCapability("avdLaunchTimeout", 100000);
     * 
     * caps.setCapability(MobileCapabilityType.APP,
     * "/Users/martinpabloleg/JavaTesting/appium/src/main/java/resources/ApiDemos-debug.apk"
     * );
     * 
     * URL url = new URL("http://0.0.0.0:4723/wd/hub");
     * 
     * AppiumDriver driver = new AndroidDriver<>(url, caps);
     * }
     */
}
